// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/*.js": {
            "when": "$(basename).ts" //hides js files when ts exist
        },
        "app/**/*.js.map": true //hide .map files
    }
}